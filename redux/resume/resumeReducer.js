import initialState from "../projects/initialStateProjects";
import initialStateResume from "./initialStateResume";

const resumeReducer = (state = initialStateResume, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default resumeReducer;
